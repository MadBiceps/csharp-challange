namespace CodingChallenge;

public class Program
{
    public static void Main(string[] args)
    {
    }

    /*
     * Task:
     * Write a C# program that uses recursion to compute the nth Fibonacci number, where n is a positive integer.
     * 
     * Requirements:
     * 
     *  - The program should prompt the user to enter a positive integer n.
     *  - The program should use a recursive function to compute the nth Fibonacci number.
     *  - The program should then print the result to the console.
     */
    public static int Fibonacci(int n)
    {
        throw new NotImplementedException();
    }

    /*
     * Task:
     * Write a C# program that takes in a list of integers and returns the longest increasing subsequence in that list.
     * A subsequence is a sequence that can be derived from another sequence by deleting some or no elements without changing the order of the remaining elements.
     * An increasing subsequence is a subsequence in which the elements are in increasing order.
     * 
     * Requirements:
     *
     *  - The program should find the longest increasing subsequence in the list.
     */
    public static List<int> FindLongestIncreasingSubsequence(int[] numbers)
    {
        throw new NotImplementedException();
    }
}